#!/bin/sh -eux

echo $(id -u)
echo $(id -g)

docker run \
       --rm \
       -v "$PWD":"$PWD" \
       -v /tmp:/tmp \
       -w "$PWD" \
       znly/protoc:0.4.0 \
       --proto_path="$PWD"/ \
       "$@"
